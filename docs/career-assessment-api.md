# Superseded career assessment contract

This document previously described `POST /api/ai/career-assessment`. That route is not registered.

Use the current [initial career assessment API contract](initial-career-assessment-api.md) for `POST /api/ai/initial-career-assessment`. The current API generates up to five career suggestions from categorized questionnaire dimensions and does not accept a caller-supplied career list.
